#include<stdio.h> 
int main()  
{  
    int a[] = {3, 4, 6, 7, 8, 9, 0, 32, 42};  
  
    int count = sizeof(a) / sizeof(int);  
  
    printf("We have %d no of elements in array\n", count);  
  
    return 0;  
} 
